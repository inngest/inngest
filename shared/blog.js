import styled from "@emotion/styled";

export const Wrapper = styled.div`
  background: var(--bg-gradient);

  header {
    padding: var(--section-padding) 0;
  }
  .header-grid .grid-line span {
    padding-top: var(--section-padding);
  }

  .image .grid-line span {
    padding-top: 4rem;
  }
`;
