/**
 * Standard Icon component props so they can be used interchangeably
 */
export type IconProps = {
  /**  Hex color or currentColor for cascading fill via CSS */
  fill?: string;
  /**  Size used for height and width */
  size?: string | number;
  className?: string;
};
