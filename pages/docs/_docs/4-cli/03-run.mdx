---
category: "CLI"
title: "`run`"
slug: "cli/run"
order: 2
---

The `inngest run` command is used to execute your funciton locally against test event payloads.

The cli uses the event triggers as defined in the `inngest.json|cue` file and generates fake events based of the event schema.

## Usage

```
$ inngest run
```
